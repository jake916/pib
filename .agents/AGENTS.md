# Workspace Rules

- **Git Push Policy**: NEVER execute `git push` automatically. Always present local changes first and ask the user for explicit confirmation before pushing.
- **Git Push Remotes**: Always ask the user which remote(s) to push to (`personal`, `client`, or both).
